spring:
  cloud:
    config:
      uri: http://localhost:8080
      username: user     #uri: http://user:password123@localhost:8080 属性的优先级比这个url的高
      password: password123
      profile: dev
      label: master   # 当configserver的后端存储是Git时，默认就是master
  application:
    name: mydream
    
    
    