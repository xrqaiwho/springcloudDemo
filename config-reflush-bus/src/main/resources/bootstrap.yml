spring:
  cloud:
    config:
      uri: http://localhost:8080
      profile: dev
      label: master   # 当configserver的后端存储是Git时，默认就是master
  application:
    name: foobar
    bus:
      trace:
        enabled: true
rabbitmq:
    host: 127.0.0.1
    port: 15672
    username: guest
    password: guest
